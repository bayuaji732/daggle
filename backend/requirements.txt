# ─── Web framework ────────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.5      # file uploads
starlette[full]>=0.27.0            # SessionMiddleware

# ─── Database ─────────────────────────────────
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# ─── Cache / Queue ────────────────────────────
celery>=5.3.0
flower>=2.0.0
redis>=5.0.0


# ─── Object Storage (S3-compatible) ───────────
boto3>=1.34.0                # works with RustFS S3 API
aiobotocore>=2.12.0          # async variant

# ─── Config / Schema ──────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.0.0     # BaseSettings with env file support

# ─── Authentication (OIDC / Keycloak) ─────────
python-jose[cryptography]>=3.3.0    # JWT decode + JWKS validation
httpx>=0.25.0                        # async HTTP client for OIDC token exchange

# ─── File processing ──────────────────────────
pandas>=2.0.0
pyarrow>=15.0.0              # Parquet support
openpyxl>=3.1.0              # Excel support
Pillow>=10.0.0               # Image processing

# ─── Utils ────────────────────────────────────
structlog==24.4.0            # structured logging
